import React from 'react';
import isTokenValid from '../Utils/isTokenValid'

const HomePage = () => {

    isTokenValid()

    return (
        <div>
            
        </div>
    );
};

export default HomePage;